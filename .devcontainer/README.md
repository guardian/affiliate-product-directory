# Dev container config

This directory contains the dev container configuration for this project. The
configuration is generated from `devenv.yaml` using [devenv](https://github.com/guardian/devenv).

Run `devenv generate` after changing `devenv.yaml`.

The shared configuration is committed to the repository. The user configuration
is generated for local personal overrides and is ignored by Git.