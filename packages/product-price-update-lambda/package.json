{
	"name": "product-price-update-lambda",
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"test": "NODE_OPTIONS=\"$NODE_OPTIONS --experimental-vm-modules\" npx jest --detectOpenHandles --config ../../jest.config.js --selectProjects product-price-update-lambda",
		"start": "tsx --env-file=.env src/run-locally.ts",
		"prebuild": "rm -rf dist",
		"build": "esbuild src/index.ts --bundle --platform=node --target=node22 --outdir=dist",
		"postbuild": "cd dist && zip -r product-price-update-lambda.zip ."
	},
	"devDependencies": {
		"esbuild": "0.28.2",
		"@aws-sdk/client-s3": "^3.1123.0"
	},
	"dependencies": {
		"zod": "^4.5.4"
	}
}
